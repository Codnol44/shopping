{% extends "base.html.twig" %}

{% block title %}{{ parent() }}: Shop {% endblock title %}

{% block body %}
    <br>
    <div id="red">
        {{ parent() }}
    </div>
    <br>
    <h1>Liste des items</h1>

    <table class="table">
        <thead>
        <tr>
            <th scope="col"></th>
            <th scope="col">Shop</th>
            <th scope="col">Catégorie</th>
        </tr>
        </thead>
        <tbody>

        {% for shop in shops %}
            <tr>
                <th scope="row"></th>
                <td><a href="{{ url('shop_detail', {'id': shop.id }) }}">{{ shop.shop }}</a></td>
                <td><a href="{{ url('shop_detail', {'id': shop.id }) }}">{{ shop.category.name }}</a></td>
                <th scope="row"><a href="{{ url('shop_delete',{'id': shop.id } ) }}">Supprimer</a></th>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <br>
    <br>
    <br>
{% endblock body %}